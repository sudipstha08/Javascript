const divs = document.getElementsByClassName("div-class"); // HTML Collection

// convert HTML collection to arrays
Array.from(divs).forEach((element) => element);

const data= document.querySelector(".classname")
